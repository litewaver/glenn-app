import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// (Optionally import global CSS or Bootstrap here)

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);